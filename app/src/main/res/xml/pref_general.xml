<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference
        android:key="silent_mode"
        android:title="@string/silent_mode"
        android:summary="@string/silent_mode_summary"
        android:defaultValue="false" />

    <EditTextPreference
        android:key="uuid"
        android:title="@string/device_uuid"
        android:summary="@string/device_uuid" />


    <ListPreference
        android:key="sync_frequency"
        android:title="@string/pref_title_sync_frequency"
        android:entries="@array/pref_sync_frequency_titles"
        android:entryValues="@array/pref_sync_frequency_values"
        android:defaultValue="180"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />

    <ListPreference
        android:key="monitor_id"
        android:title="@string/pref_monitor_id"
        android:defaultValue="-1"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />


    <Preference
        android:key="version"
        android:selectable="false"
        android:title="Version"
        android:summary="@string/version"/>

    <Preference
        android:title="Close app"
        android:key="close"
        android:summary="close"/>

</PreferenceScreen>
